{% extends 'layout.twig' %}

{% block title %}Suppression de l'article{% endblock %}

{% block content %}

    {#    <section class="banner offset-next-element" style="background-image: url('./ressources/images/vector-shapes-2.jpg');">#}
    <section class="banner offset-next-element" style="background-image: url('./ressources/images/{{ post.image }}');">
        <div class="wrapper">
            <div class="title">
                <h1>Suppression de l'article</h1>
            </div>
        </div>
        <img class="vector-shape vector-shape-bottom" src="./ressources/images/vector-bottom-right-white.svg" alt="">
    </section>
    <section class="delete-post position-relative">
        <div class="wrapper">
            <div class="page management">
{#                <p><a href="index.php?action=post&id={{ post.identifier }}">Retour à l'article</a></p>#}
            <h2 class="title">Souhaitez-vous vraiment supprimer l'article ?</h2>

                <div class="single">
                    <div class="post">
{#                        {{ dump(post) }}#}
                        <div class="header">
                            {#                            <div class="title">#}
                            <h3 class="title"><i class="fas fa-newspaper"></i>{{ post.title }}</h3>
                            {% if session.role == 'Admin' %}
                                <div class="actions">
                                    <a class="hide" href="index.php?action=post&id={{ post.identifier }}"><i class="fas fa-eye"></i>Voir</a>
                                    <a class="edit" href="index.php?action=updatePost&id={{ post.identifier }}"><i class="fas fa-edit"></i>Modifier</a>
{#                                    <a class="delete" href="index.php?action=deletePost&id={{ post.identifier }}"><i class="fas fa-trash"></i>Supprimer</a>#}
                                </div>
                            {% endif %}
                            {#                            </div>#}
                        </div>
                        {#                        <div class="content">#}
                        <p class="description">{{ post.content }}</p>
                        <p><span class="author">{{ post.username }}</span> le <span class="date">{{ post.creationDate }}</span></p>
                        {#                            <a class="button-secondary button-blue" href="index.php?action=post&id={{ post.identifier }}">Lire l'article</a>#}
                        {#                        </div>#}
                    </div>
                </div>


                <form action="index.php?action=deletePost&id={{ post.identifier }}" method="post">
{#                    <div class="post">#}
{#                        <div class="title">#}
{#                            <p><strong>{{ post.title }}</strong> le {{ post.creationDate }}</p>#}
{#                        </div>#}
{#                        <div class="content">#}
{#                            <p class="description">{{ post.content }}</p>#}
{#                        </div>#}
{#                    </div>#}
                    <div>
                        <input type="submit" value="Supprimer" />
                    </div>
                </form>

            </div>
        </div>
        <img class="vector-shape vector-shape-bottom vector-shape-behind" src="./ressources/images/vector-bottom-right-dark-grey.svg" alt="">
    </section>
{% endblock %}