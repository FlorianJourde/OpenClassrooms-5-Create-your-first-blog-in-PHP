<!doctype html>
<html lang="fr">
<head>
    {% block head %}{% endblock %}
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="ressources/css/main.css" rel="stylesheet" />
    <title>Document</title>
</head>
<body>
    <div class="wrapper">
        {{ dump(session) }}
        {% if session.is_authenticated != true %}
            <a class="button-primary" href="index.php?action=register">S'enregistrer</a>
            <a class="button-primary" href="index.php?action=login">Se connecter</a>
        {% else %}
            <h2>Bonjour {{ session.username }} !</h2>
            <a class="button-primary" href="index.php?action=logoutUser">Se déconnecter</a>
        {% endif %}
        {% block content %}{% endblock %}
    </div>
</body>
</html>